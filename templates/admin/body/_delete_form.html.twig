<form method="post" action="{{ path('app_admin_body_delete', {'id': body.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ body.id) }}">
    <button class="btn">Delete</button>
</form>
